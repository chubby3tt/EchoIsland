{
  "name": "echoisland-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "crates/*"
  ],
  "scripts": {
    "dev": "npm run --prefix apps/desktop tauri dev",
    "build": "npm run --prefix apps/desktop tauri build",
    "desktop:build": "npm run --prefix apps/desktop tauri build"
  }
}
